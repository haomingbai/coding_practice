#include <algorithm>
#include <iostream>
#include <list>
#include <set>
#include <unordered_set>
#include <vector>

using namespace std;

// void build_tree(int left, int right, int p);

int main()
{
    int n, m, s;
    cin >> m >> n >> s;

    for (auto &&i : person)
    {
        cout << i << '\n';
    }
    
}